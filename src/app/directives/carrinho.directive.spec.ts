import { CarrinhoDirective } from './carrinho.directive';

describe('CarrinhoDirective', () => {
  it('should create an instance', () => {
    const directive = new CarrinhoDirective();
    expect(directive).toBeTruthy();
  });
});
